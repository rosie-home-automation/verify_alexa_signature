{
  "name": "alexa_verify_signature",
  "version": "0.1.0",
  "description": "Verify Alexa requests using the signature, signature cert url, and request body.",
  "author": "Scott Olson",
  "license": "MIT",
  "keywords": [
    "Amazon",
    "Echo",
    "Alexa"
  ],
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "echo \"Error: no start specified\" && exit 1"
  },
  "dependencies": {
    "bluebird": "^2.10.0",
    "normalizeurl": "^0.1.3",
    "request": "^2.61.0",
    "x509": "^0.2.2"
  }
}
